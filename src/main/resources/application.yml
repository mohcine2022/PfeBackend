spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pfe_database
    username: postgres
    password: Password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
